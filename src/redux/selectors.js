export const getTopics = state => {
    return state.topics;
}

export const getDifficulties = state => {
    return state.difficulties;
}

export const getQuestions = state => {
    return state.questions;
}